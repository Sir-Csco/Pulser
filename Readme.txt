Pulser pings a server to check if server is live or offline.

if server is offline
  Pulser sends an email to the client informing them that the server is online
  
 if server is offline
  pulser sends and email to the client informing them that the client is offline and also
  sends an IPStatus
  
  Arguments
    1. client email address
    2. server address

